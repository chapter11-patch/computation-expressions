image: vvakame/review
script:
  - env
  - ruby --version
  - gem list
  - rake clean pdf
deploy:
  ssh:
    target: mzp@codefirst.org:/opt/nginx/html/mzp/ch11-patch/${CI_BRANCH}.tar
    artifacts:
      - book.pdf
      - book-print.pdf
    cmd: /home/mzp/ch11.sh ${CI_BRANCH}
